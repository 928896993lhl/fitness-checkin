# 产品需求文档：微信小程序健身打卡应用

## 1. 项目信息

- **语言**: 中文
- **编程语言**: Vite + React + MUI + Tailwind CSS（微信小程序框架）
- **项目名称**: `fitness_checkin_miniapp`
- **原始需求复述**: 用户需要一款微信小程序，支持微信登录，可以创建或加入健身打卡圈子。圈子发起人设定周期（如一周）和运动量目标（总时间、每天时间、圈子总目标）。成员每天打卡汇报运动时长，需上传运动照片作为证明。打卡时间不限，同一天可多次打卡。圈子人数限制2-8人，成员不允许退出。暂不考虑激励、变现、押金等金钱相关功能。每日汇总推送圈子成员，周期结束前两天提醒未完成目标的成员。数据统计可视化暂不需要，但需保留数据结构以支持未来查询。圈子与历史分开，圈子是成员集合，周期是计划，一个圈子可开启多轮计划。发起人也是成员，需满足运动目标。

## 2. 产品定义

### 产品目标
1. **提供基于微信小程序的健身打卡平台**：用户通过微信登录，创建或加入健身圈子，参与周期性运动计划。
2. **促进用户坚持运动习惯**：通过圈子成员互相监督、每日打卡和汇总提醒，提高用户运动积极性。
3. **实现简洁的打卡流程和基础数据管理**：支持运动时长记录、照片上传、圈子管理等功能，为未来数据统计奠定基础。

### 用户故事
1. **作为** 用户，**我想要** 通过微信快速登录小程序，**以便** 无需额外注册即可使用。
2. **作为** 用户，**我想要** 创建或加入健身打卡圈子，**以便** 与朋友一起设定运动目标并互相监督。
3. **作为** 圈子发起人，**我想要** 设定周期计划（时间周期、运动量目标），**以便** 为圈子成员提供明确的运动目标。
4. **作为** 圈子成员，**我想要** 每天打卡并上传运动照片，**以便** 记录自己的运动情况并满足圈子目标。
5. **作为** 圈子成员，**我想要** 收到每日汇总和周期结束提醒，**以便** 了解自己和圈子的完成进度。
6. **作为** 用户，**我想要** 查看圈子历史计划数据，**以便** 回顾过去的运动成果。

## 3. 技术规范

### 需求池

#### P0（必须有）
1. **微信登录**：使用微信授权登录，获取用户基本信息。
2. **圈子管理**：
   - 创建圈子（需设定圈子名称、人数限制2-8人）
   - 加入圈子（通过邀请码或分享）
   - 查看圈子成员列表
   - 成员不允许退出圈子
3. **周期计划管理**：
   - 发起人设定周期（如一周）和运动量目标（总时间、每天时间、圈子总目标）
   - 计划启动、进行中、已结束状态管理
   - 一个圈子可开启多轮计划
4. **打卡功能**：
   - 运动时长记录（每次不低于10分钟，可多次打卡）
   - 运动照片上传（简单证明，不强制检查）
   - 运动类型自定义
   - 打卡时间不限
5. **数据存储**：
   - 用户信息、圈子信息、计划信息、打卡记录等数据结构
   - 支持未来数据查询和分析

#### P1（应该有）
1. **每日汇总推送**：每日汇总圈子成员运动情况，通过微信服务通知推送。
2. **周期结束提醒**：周期结束前两天提醒未完成目标的成员。
3. **运动类型管理**：支持自定义运动类型。
4. **圈子历史计划**：查看圈子历史计划及完成情况。

#### P2（可以有）
1. **数据可视化预留**：数据结构支持未来统计图表展示。
2. **多设备同步**：基于微信登录天然支持多设备同步。
3. **圈子模板**：未来可考虑预设圈子模板。

### UI 设计草稿

#### 关键界面
1. **首页**：
   - 微信登录入口
   - 我的圈子列表（当前参与的圈子）
   - 快速打卡入口

2. **圈子详情页**：
   - 圈子信息（名称、人数、当前计划状态）
   - 成员列表及运动进度
   - 当前计划详情（周期、目标、剩余时间）
   - 历史计划入口

3. **打卡页面**：
   - 运动时长输入（分钟）
   - 运动类型选择（可自定义）
   - 运动照片上传
   - 提交打卡按钮

4. **计划创建页面**（发起人）：
   - 周期选择（天/周）
   - 运动量目标设置（总时间、每天时间、圈子总目标）
   - 每次运动最低时长设置（默认10分钟）
   - 启动计划按钮

5. **个人中心**：
   - 个人信息
   - 我的圈子列表
   - 运动历史记录

### 数据模型建议

#### 核心实体
1. **用户表 (Users)**：
   - `user_id` (主键)
   - `openid` (微信openid)
   - `nickname` (昵称)
   - `avatar_url` (头像)
   - `created_at` (创建时间)

2. **圈子表 (Circles)**：
   - `circle_id` (主键)
   - `name` (圈子名称)
   - `creator_id` (创建者user_id)
   - `max_members` (最大成员数，2-8)
   - `invite_code` (邀请码)
   - `created_at` (创建时间)
   - `status` (状态：活跃/归档)

3. **圈子成员表 (Circle_Members)**：
   - `id` (主键)
   - `circle_id` (圈子ID)
   - `user_id` (用户ID)
   - `joined_at` (加入时间)
   - `role` (角色：成员/发起人)

4. **计划表 (Plans)**：
   - `plan_id` (主键)
   - `circle_id` (所属圈子ID)
   - `name` (计划名称)
   - `start_date` (开始日期)
   - `end_date` (结束日期)
   - `total_duration_goal` (总运动时长目标，分钟)
   - `daily_duration_goal` (每天运动时长目标，分钟)
   - `circle_total_goal` (圈子总运动时长目标，分钟)
   - `min_duration_per_checkin` (每次打卡最低时长，默认10分钟)
   - `status` (状态：未开始/进行中/已结束)
   - `created_at` (创建时间)

5. **打卡记录表 (Checkin_Records)**：
   - `record_id` (主键)
   - `plan_id` (计划ID)
   - `user_id` (用户ID)
   - `duration` (运动时长，分钟)
   - `exercise_type` (运动类型)
   - `photo_url` (运动照片URL)
   - `checkin_time` (打卡时间)
   - `created_at` (记录创建时间)

#### 关系说明
- 一个圈子可以有多个成员（一对多）
- 一个圈子可以有多个计划（一对多）
- 一个计划属于一个圈子（一对一）
- 一个用户可以属于多个圈子？根据需求，用户暂时只能参加一个圈子。
- 一个计划可以有多个打卡记录（一对多）
- 一个用户在一个计划中可以有多条打卡记录（一对多）

### 开放问题
1. **微信服务通知权限**：如何获取用户订阅消息权限？需要用户主动订阅。
2. **图片存储**：运动照片存储方案（微信云存储或第三方服务）。
3. **推送时机**：每日汇总推送的具体时间点（如晚上8点）？
4. **圈子人数限制**：硬性限制2-8人，是否允许发起人调整？
5. **运动时长验证**：虽然靠自觉，是否需要设置最大运动时长（如24小时）避免误填？
6. **计划周期灵活性**：周期支持天、周、月等多种单位？还是固定为周？
7. **圈子名称重复**：是否允许同名圈子？
8. **邀请机制**：通过邀请码还是直接分享链接？
9. **数据保留期限**：历史数据永久保留还是设定保留期限？
10. **并发打卡**：同一天多次打卡，数据如何合并显示？

## 4. 总结

本PRD定义了一个基于微信小程序的健身打卡应用，核心功能围绕圈子、周期计划和打卡展开。通过P0/P1/P2优先级划分，确保核心功能优先实现，为后续迭代奠定基础。